apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # El UID es fijo porque los dashboards versionados lo referencian.
    uid: godml-prometheus
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
